# Implementation Plan: Baseline AI Opponents

**Branch**: `010-baseline-bots` | **Date**: 2026-05-25 | **Spec**: [spec.md](spec.md)
**Input**: Feature specification from `/specs/010-baseline-bots/spec.md`

## Summary

Add two stateless baseline AI opponents — **Random** and **Greedy** — to the CYCLES engine as pure decision functions `(GameState) → Move`. Integrate them into the React UI as selectable difficulty levels with player-role selection (P1/P2). Provide a headless simulation harness for automated regression testing (1000-game stability runs). The feature is scoped entirely within existing architectural boundaries: no new dependencies, no engine purity violations, no persistent state.

## Technical Context

**Language/Version**: TypeScript 5.x (strict mode, `noUncheckedIndexedAccess`, `exactOptionalPropertyTypes`)
**Primary Dependencies**: React 18.x, Vite 5.x, pure CSS (no new dependencies)
**Storage**: N/A (stateless bots, no persistence)
**Testing**: Vitest + `@testing-library/react` + `fast-check`
**Target Platform**: Web (Cloudflare Pages), desktop browsers, mobile PWA
**Project Type**: Web application (single-page app with PWA support)
**Performance Goals**: Bot move selection < 100ms; 1000-game simulation < 60s total
**Constraints**: Engine purity (zero React imports in `src/core/`); immutability by default; `src/core/` ≥ 90% coverage
**Scale/Scope**: 2 bot strategies, 1 simulation harness, 1 setup UI screen, minor hook/component extensions

## Constitution Check

*GATE: Must pass before Phase 0 research. Re-check after Phase 1 design.*

| Principle | Status | Notes |
|-----------|--------|-------|
| I. Engine Purity | **PASS** | Bots are pure `(GameState) → Move` functions in `src/core/bots/`. No React, no DOM, no side effects. |
| II. Test-First Discipline | **PASS** | All bot code driven by failing tests. Property-based tests for Random uniformity and Greedy determinism. |
| III. UI/Engine Separation | **PASS** | Bot logic in core; turn orchestration (when to invoke bot) lives in UI hooks only. |
| IV. Pre-Commit Quality Gates | **PASS** | Standard Biome + tsc + Vitest gates apply. |
| V. Accessibility by Default | **PASS** | New setup screen must use 44×44px touch targets, visible focus, color+shape labels. |
| VI. Immutability by Default | **PASS** | Bot functions read `GameState`, never mutate it. Return new `Move` objects. |
| VII. Canonical Rules Fidelity | **PASS** | Bots consume existing `legalPlacements`, `legalJoins`, `applyMove`, `computeFinalScore`. No rule changes. |

**Re-check after Phase 1**: No new violations introduced. All design decisions preserve existing boundaries.

## Project Structure

### Documentation (this feature)

```text
specs/010-baseline-bots/
├── plan.md              # This file
├── research.md          # Phase 0 output — bot interface design rationale
├── data-model.md        # Phase 1 output — types for bots, simulation, setup
├── quickstart.md        # Phase 1 output — how to run simulations / add a bot
├── contracts/
│   └── bot-api.md       # Bot function contract
└── tasks.md             # Phase 2 output (generated by /speckit.tasks)
```

### Source Code (repository root)

```text
src/
├── core/
│   ├── bots/
│   │   ├── random.ts           # RandomBot: uniform legal-move selector
│   │   ├── greedy.ts           # GreedyBot: max immediate score delta
│   │   ├── simulate.ts       # Headless simulation harness
│   │   └── index.ts            # Bot exports & types
│   ├── __tests__/
│   │   ├── behavior/
│   │   │   └── bot-strategies.test.ts   # Random uniformity, Greedy score-maximization
│   │   └── bots/
│   │       └── simulation.test.ts       # 1000-game harness regression
│   └── [existing files]
└── ui/
    ├── hooks/
    │   ├── useGameSession.ts     # [existing — no bot logic]
    │   └── useBotGame.ts         # NEW: wraps useGameSession, auto-invokes bot on its turn
    ├── components/
    │   └── SetupScreen.tsx       # NEW: opponent + player-role selection
    ├── pages/
    │   └── GamePage.tsx          # [minor: accept bot-aware session hook]
    └── App.tsx                   # [minor: integrate SetupScreen]
```

**Structure Decision**: Bots and simulation are core engine modules (`src/core/bots/`). UI orchestration (`useBotGame.ts`, `SetupScreen.tsx`) lives in `src/ui/` and consumes core via the `@core/` alias. This preserves the existing UI/engine boundary.

## Complexity Tracking

> No Constitution violations requiring justification. The feature stays within the existing two-layer architecture (core + ui) and adds no new project boundaries.

---

## Phase 0: Outline & Research

**Unknowns resolved**:

1. **Bot function signature**: `(state: GameState, player: Player) => Move` vs `(state: GameState) => Move`. Resolution: `(state: GameState) => Move` is sufficient because `GameState.currentPlayer` tells us whose turn it is. The bot always moves for `currentPlayer`.
2. **Greedy score evaluation**: `computeFinalScore` counts total heads/tails. Greedy needs "score for current player before vs after." Resolution: add `scoreForPlayer(state, player)` helper in core, reuse in greedy evaluation.
3. **Simulation runner location**: Should it be a CLI script or a test harness? Resolution: both. `src/core/bots/simulate.ts` exports a pure `runSimulation(config)` function usable by tests and a future CLI wrapper. The 1000-game regression lives as a core behavior test.
4. **Bot turn timing in UI**: Immediate (synchronous) vs delayed. Resolution: `useBotGame.ts` triggers bot via `useEffect` with a 2000ms minimum delay for UX, but the bot function itself is synchronous. Tests bypass the delay.

**Research artifacts**: See [research.md](research.md) for detailed rationale on each decision.

## Phase 1: Design & Contracts

### Data Model

See [data-model.md](data-model.md) for full entity definitions.

Key additions:
- `BotFunction` type: `(state: GameState) => Move`
- `BotStrategy` union: `"random" | "greedy"`
- `SimulationConfig` / `SimulationResult` for headless runs
- `GameSetupOptions` for UI game configuration

### Interface Contracts

See [contracts/bot-api.md](contracts/bot-api.md) for the formal bot function contract.

Summary:
- **Input**: A valid `GameState` where `hasLegalMoves(session)` is true (caller guarantees at least one legal move exists).
- **Output**: A `Move` object that, when passed to `applyMove(state, move)`, produces a valid next state without throwing.
- **Behavior**: Function is pure, synchronous, and returns within 100ms.
- **Error handling**: If no legal moves exist, the bot is not called (engine handles forced pass). If called with invalid state, bot may throw.

### Agent Context Update

`AGENTS.md` plan reference updated to point to this plan file.

### Quickstart

See [quickstart.md](quickstart.md) for developer onboarding: running simulations, adding a new bot strategy, debugging bot decisions.
